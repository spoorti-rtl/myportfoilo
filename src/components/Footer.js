import React from "react";

function Footer() {
  return (
    <footer className="footer">
      <p>© {new Date().getFullYear()} MyPortfolio | Built with React</p>
    </footer>
  );
}

export default Footer;